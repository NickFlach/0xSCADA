import { Router } from "express";

const router = Router();

// Ubiquity routes - placeholder implementation
router.get("/", async (req, res) => {
  res.json({ 
    message: "Ubiquity routes - implementation pending",
    services: []
  });
});

export default router;