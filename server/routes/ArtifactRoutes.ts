import { Router } from "express";

const router = Router();

// Artifact routes - placeholder implementation
router.get("/", async (req, res) => {
  res.json({ 
    message: "Artifact routes - implementation pending",
    artifacts: []
  });
});

export default router;