apiVersion: v1
kind: Service
metadata:
  name: opcua-driver
  namespace: oxscada-protocols
  labels:
    app.kubernetes.io/name: opcua-driver
    app.kubernetes.io/part-of: oxscada
spec:
  type: ClusterIP
  ports:
    - port: 4840
      targetPort: 4840
      name: opcua
  selector:
    app.kubernetes.io/name: opcua-driver
