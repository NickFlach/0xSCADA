apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: oxscada
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/part-of: oxscada
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgresql
  selector:
    app.kubernetes.io/name: postgresql
